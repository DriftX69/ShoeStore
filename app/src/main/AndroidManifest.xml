<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onlinestore">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineStore">
        <activity android:name=".Produk6" />
        <activity android:name=".Produk5" />
        <activity android:name=".Produk4" />
        <activity android:name=".Produk3" />
        <activity android:name=".Produk2" />
        <activity android:name=".produk1" />
        <activity android:name=".Payment" />
        <activity android:name=".FeedBack" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>